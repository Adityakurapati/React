import React from 'react';

const headerStyle={
               backgroundColor: 'royalblue',
               color: '#fff',
               padding: '-3px'
};
const Header=() => (
               <header style={ headerStyle }><h1>Groceries List</h1> </header>
);

export default Header